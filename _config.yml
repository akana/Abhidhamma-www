# Site Build Settings

# jekyll-paginate configuration
pagination:
  enabled: true
  per_page: 10
  permalink: '/page/:num/'
  limit: 0
  sort_field: 'date'
  
# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate-v2, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

sass:
  sass_dir: assets/css
  style: :nested

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "អភិធម្មសង្គហ"
description:	       "បង្កើតជាមួយ [Millennial](https://lenpaul.github.io/Millennial/documentation/welcome-to-millennial.html) នៅ ព.ស ២៥៦៥ គ.ស ២០២១"
author:              "ឧបាសក ម៉ៅ អាកាណា"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
